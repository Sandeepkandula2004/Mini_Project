import React from "react";
import WebcamCapture from "../components/Cam";

const Upload = () =>{
    return (
        <div>
            <WebcamCapture/>
        </div>
    );
};

export default Upload;